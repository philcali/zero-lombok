    @Override
    public boolean equals(final Object object) {
        if (Objects.isNull(object) || !(object instanceof {{elementName}})) {
            return false;
        }
        final {{elementName}} other = ({{elementName}}) object;
        return {{#fields}}Objects.deepEquals({{fieldName}}, other.{{methodName}}()){{#unless @last}}
            && {{/unless}}{{/fields}};
    }

    @Override
    public int hashCode() {
        return Objects.hash({{#fields}}
            {{fieldName}}{{#unless @last}}, {{/unless}}{{/fields}});
    }
